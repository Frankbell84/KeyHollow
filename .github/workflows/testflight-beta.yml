name: KeyHollow V2 Beta Upload (Retired)

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    if: ${{ false }}
    runs-on: macos-26
    steps:
      - name: Beta delivery is intentionally disabled
        run: |
          echo 'The stale push-to-upload beta path is retired.'
          exit 1
